#include <iostream>
#include <string>
#include <algorithm>
#include <limits>
#include <array>
#include <cmath>
#include <sstream>
using namespace std;
string decimalConverter(int decOctet);
int query(int i);
void subnetmaskCalc(int hostQuery);
string binaryConverter(string binaryArray, int size);
